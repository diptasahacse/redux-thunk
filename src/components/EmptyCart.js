import React from "react";

const EmptyCart = () => {
  return (
    <div className="empty-cart">
      <h4 className="text-center m-0">Cart is empty</h4>
    </div>
  );
};

export default EmptyCart;
